# supervoxtral — Exemple de fichier .env
# ------------------------------------------------------------
# Copiez ce fichier en `.env` à la racine du projet et remplissez
# les variables nécessaires. Le chargeur `python-dotenv` lira ce
# fichier automatiquement au démarrage de l'outil.
#
# IMPORTANT: Ne commitez jamais votre fichier `.env` avec des clés réelles.
# ------------------------------------------------------------

# --- Clés API ---
# Clé Mistral (requise si vous utilisez le provider Mistral/Voxtral)
MISTRAL_API_KEY=changeme_mistral_key

# Clé OpenAI (optionnelle, uniquement si vous utilisez le provider Whisper)
OPENAI_API_KEY=

# --- Réglages par défaut (optionnels) ---
# Provider par défaut: mistral | whisper
SVX_DEFAULT_PROVIDER=mistral

# Modèle Mistral Voxtral par défaut
SVX_MISTRAL_MODEL=voxtral-small-latest

# Format à envoyer au provider après enregistrement: wav | mp3 | opus
SVX_DEFAULT_FORMAT=wav

# Taux d'échantillonnage et nombre de canaux pour l'enregistrement
SVX_SAMPLE_RATE=16000
SVX_CHANNELS=1

# Conserver le WAV après conversion (true/false)
SVX_KEEP_WAV=true

# Niveau de logs: DEBUG | INFO | WARNING | ERROR
SVX_LOG_LEVEL=INFO

# Répertoires (laisser vide pour valeurs par défaut)
SVX_RECORDINGS_DIR=recordings
SVX_TRANSCRIPTS_DIR=transcripts
SVX_LOGS_DIR=logs

# Binaire ffmpeg (laisser "ffmpeg" si présent dans le PATH)
FFMPEG_BIN=ffmpeg

# --- Notes ---
# - Sous macOS/Linux, vous pouvez aussi exporter ces variables dans votre shell:
#     export MISTRAL_API_KEY="votre_cle"
# - Si ffmpeg n'est pas installé, sous macOS:
#     brew install ffmpeg
# - Redémarrez le terminal si nécessaire après avoir défini des variables d'environnement.
